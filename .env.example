# These frontend-safe public Supabase values belong in .env.local, which must not be committed.
# Never place a Supabase service-role key in frontend code or environment files committed to this repository.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
